# 作業中タスク（2026-07-04 のフィードバック対応）

> 使用制限をまたいで再開するための進捗管理。完了したら `[x]` にしてコミットする。
> 再開時はこのファイルと `git log` を確認してから続きを実装すること。

## A. ノート

- [ ] A1: +ボタンはポップアップでなく、ツリーに「新しいノート」「新しいフォルダ」を
      直接追加し、Windows のようにその場で名前入力（インライン rename）
- [ ] A2: エディタ右上の「名前変更」ボタンを削除。ツリーで**選択済みの項目を
      もう一度クリック**すると名前変更（Windows 流）
- [ ] A3: エディタ右上の「削除」ボタンを削除。ツリーの項目を**右クリック**で
      「名前を変更 / 削除」のコンテキストメニュー
- [ ] A4: 既存ノートを開いたら**プレビューが既定**、新規作成直後は**編集が既定**

## B. チケット

- [ ] B1: 左の分類レールは表示/非表示を切替できる（トグルボタン）
- [ ] B2: 分類レールをカレンダーのカテゴリとは**別の分類**にする。
      新テーブル `ticket_groups`（migration v5）+ `tasks.group_id`。
      既定は「自学習」「プロジェクト」（他案: 仕事 / 生活 / 保守・雑務）。
      レールから追加・名前変更（ダブルクリック）・削除（ホバー×+確認）
- [ ] B3: カンバンの完了列は**当月に完了したものだけ**表示（月初でクリア）
- [ ] B4: ガント左ペインをドラッグで縮小可能に。最小幅は設定で変更できる
- [ ] B5: 削除は必ずアプリ内の確認ダイアログを出す（window.confirm は
      環境によって表示されない可能性があるため自前モーダルに置換）
- [ ] B6: 「履歴」タブ新設。完了チケット/タスクを年→月でグルーピングし、
      月ごと・年ごとに何を成し遂げたか振り返れるビュー（件数・実績合計付き）
- [ ] B7: カンバンは**子チケットのみ**表示（親チケットは載せない）。
      ガントは親+子の現状維持
- [ ] B8: 「チケット」タブ新設（Jira/Atlassian 風）。親子チケットを包括管理、
      詳細（説明・ステータス・分類・カテゴリ・期間・見積・子タスク）を編集

## C. スケジュール

- [ ] C1: カテゴリのチェックボックスを廃止し、チップ（1行に2〜3個折返し）に。
      クリックで表示/非表示をトグル（非表示はグレーアウト）

## 実装順（コミット単位）

1. C1 + A4 + B5（確認ダイアログ基盤）+ B3
2. A1〜A3（ノートツリー UX）
3. B2 + B1 + B7（分類テーブル・レール・カンバン子のみ）
4. B6（履歴タブ）
5. B8（チケットタブ）
6. B4（ガント左ペインのリサイズ）
